/** Descripteur vide pour les appels au service titulaire (complété plus tard côté métier). */
export const EMPTY_CHARGE_DESCRIPT = {
  objectif: [] as { title: string; contenu: string[] }[],
  methodologie: [] as { title: string; contenu: string[] }[],
  mode_evaluation: [] as { title: string; contenu: string[] }[],
  penalties: [] as { title: string; contenu: string[] }[],
  ressources: [] as { title: string; contenu: string[] }[],
  plan_cours: [] as { title: string; contenu: string[] }[],
};
