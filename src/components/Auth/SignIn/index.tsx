"use client";

import { OtpSignInFlow } from "./OtpSignInFlow";

/**
 * Parcours de connexion (OTP) — préférer l’import direct de `OtpSignInFlow` sur la page.
 */
const Signin = () => {
  return <OtpSignInFlow />;
};

export default Signin;
